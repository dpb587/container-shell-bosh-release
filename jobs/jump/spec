name: jump
templates:
  bin/shell: bin/shell
  bin/pre-start: bin/pre-start
properties:
  home_dir:
    description: "A path for home directories"
    default: /var/vcap/store/jump
  default_container:
    description: "Default container when not specified"
    default: "ubuntu:16.04"
  default_workdir:
    description: "Default container working directory when not specified"
    default: "/root"
  env:
    description: "Custom environment variables to propagate"
    default: {}
  users:
    description: "Array of hashes with `user` and `public_keys`"
    default: []
